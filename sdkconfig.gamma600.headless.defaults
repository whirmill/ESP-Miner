# Gamma 600 (no PSRAM) - headless build (no display UI, no AxeOS static files).
#
# Use via:
#   idf.py -DSDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.gamma600.defaults;sdkconfig.gamma600.ipv6.defaults;sdkconfig.gamma600.headless.defaults" build
#
CONFIG_ESP_MINER_HEADLESS=y

